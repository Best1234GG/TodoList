import React from 'react'

const AddList = () => {
    return (
        <div>
            <input
                placeholder='Add a new task...'
                required
            />
            <button onClick={ }>+ Add</button>
        </div>
    )
}

export default AddList
